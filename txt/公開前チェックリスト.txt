ポケグラフ 公開前チェックリスト

ビルド
- `Build-PokemonData.ps1` を実行した
- `Build-StaticPages.ps1` を実行した
- `docs/` が最新になっている

URL / メタ
- `SiteUrl` を本番URLで指定して静的生成した
- `title` がポケモンごとに変わる
- `meta description` がポケモンごとに変わる
- `canonical` が正しい
- `og:url` が正しい
- `og:image` が正しい
- `sitemap.xml` が生成されている
- `robots.txt` が生成されている

主要ページ
- `/` が開く
- `/pokemon/garchomp/` が開く
- 直アクセスでページが開く

導線
- 検索候補から個別ページへ遷移できる
- 完全一致検索で個別ページへ遷移できる
- 顔出し演出クリックで個別ページへ遷移できる
- 注目のポケモンから個別ページへ遷移できる

表示内容
- 基本情報が表示される
- 画像が表示される
- 弱点が表示される
- 能力グラフが表示される
- 適正が表示される
- 起点作成が表示される
- 覚える技が表示される
- 技範囲ヒートマップが表示される
- ひとくちメモが表示される

スマホ / PC
- PC表示で大きく崩れない
- スマホ表示で大きく崩れない
- 注目のポケモンの配置が意図通り
- ヒートマップのスクロールが意図通り

データ整合
- 特性名に未定義がない
- 技名に未定義がない
- `key` の重複がない
- `imageKey` の参照先画像がある

公開後確認
- GitHub Pages のURLで開ける
- 外部リンクから個別ページへ飛べる
- ページタイトルがブラウザで正しく出る
